# Repository Configuration

This directory groups repository-level configuration files that are needed by the build but are not part of the public framework API.

Current contents:

- [`checkstyle/checkstyle.xml`](checkstyle/checkstyle.xml): Checkstyle rules used by the Maven verification lifecycle.
